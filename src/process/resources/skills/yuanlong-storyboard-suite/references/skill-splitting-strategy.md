# Skill Splitting Strategy

Default: keep one master skill (`yuanlong-storyboard-suite`).

Split only when module is independently reusable:
- style-only assistant
- export-only assistant
- validator-only assistant
